package cache

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func TestClient_Set(t *testing.T) {
	Convey("Given redis client is set up", t, func() {


		Convey("When a valid session is provided", func() {


			Convey("Then the session is stored in redis", func() {

			})
		})
	})
}
